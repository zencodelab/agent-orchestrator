# Loaded by `vite --mode demo` (npm run dev:demo / build:demo).
# Turns on the client-side demo backend so the app runs with no server
# (used for the standalone GitHub Pages deploy).
VITE_DEMO_MODE=true
